describe("Frame", function() {
  var frame;

beforeEach(function() {
  frame = new Frame;
});

  describe('points', function() {
    it('initiates at 0', function() {
      expect(frame.points).toBe(0)
    });
  });



});
